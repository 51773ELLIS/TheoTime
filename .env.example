# TheoTime Environment Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Authentication
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Database Configuration
# For local development, use relative path
# For production/Docker, use absolute path like /app/database/theotime.sqlite
DB_PATH=./database/theotime.sqlite

# Frontend API URL (for Vite build)
# In development, Vite proxy handles this automatically
# In production, use absolute URL if frontend is on different domain
VITE_API_URL=/api

# OpenAI API (Optional - for AI Assistant features)
# Note: OpenAI API key is configured through the Settings UI in the application
# and stored in the database settings table, not as an environment variable.
# To enable AI features:
# 1. Get your API key from https://platform.openai.com/api-keys
# 2. Log in to TheoTime as a parent/admin
# 3. Go to Settings > AI Configuration
# 4. Enter your OpenAI API key and select model (default: gpt-3.5-turbo)
